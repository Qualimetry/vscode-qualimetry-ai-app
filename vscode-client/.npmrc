# This is a public, open-source package: always resolve from the public npm
# registry. Project-level config overrides any machine/user-profile registry
# (JFrog/Nexus mirrors), so package-lock.json never records private resolved
# URLs that public CI runners cannot authenticate to (npm E401).
registry=https://registry.npmjs.org/
